require 'rails_helper'
=begin
RSpec.describe "TagLists", type: :request do
  describe "GET /tag_lists" do
    it "works! (now write some real specs)" do
      get tag_lists_path
      expect(response).to have_http_status(200)
    end
  end
end
=end
