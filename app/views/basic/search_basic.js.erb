var records_div = ".<%= @records.klass.to_s.pluralize.downcase%>";

$( records_div ).fadeOut( "fast", function() {

    content = $("<%= escape_javascript( render 'basic/records', records: @records ) %>")

    $(records_div).replaceWith(function(){
      return $(content).hide().fadeIn("fast");
    });

});
