<div class="d-none d-md-block">
  <div class="d-flex justify-content-center">
    <%= paginate records, window: 2, outer_window: 1, params: {controller: 'basic', action: 'search_basic', search_inputs: params.merge(model: records.klass.to_s).permit!} , theme: 'prevnext', remote: true %>
  </div>
</div>

<div class="d-md-none">
  <div class="d-flex justify-content-center">
    <%= paginate records, window: 1, params: {controller: 'basic', action: 'search_basic', search_inputs: params.merge(model: records.klass.to_s).permit!}, remote: true %>
  </div>
</div>
